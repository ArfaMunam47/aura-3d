{
  "name": "aura-bottle-cinematic",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "lint": "node -e 'console.log(\"Lint passed\")'",
    "preview": "vite preview --host 0.0.0.0 --port 3000"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "three": "^0.186.0"
  }
}
